@using Roadkill.Core.Configuration;
@{
	IConfigurationContainer config = ((Roadkill.Core.Controllers.ControllerBase)ViewContext.Controller).Configuration;
	if (config.ApplicationSettings.Installed && !config.ApplicationSettings.UpgradeRequired)
	{
		Layout = config.SitePreferences.ThemePath + "/Theme.cshtml";
	}
}