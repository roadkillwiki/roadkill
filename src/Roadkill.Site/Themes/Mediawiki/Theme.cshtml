@inherits RoadkillLayoutPage
<!DOCTYPE html>
<html>
	<head>
		<title>@ViewBag.Title</title>
		@Html.Partial("HeadContent")
	</head>
<body>

@using (Html.BeginForm("Search", "Home", FormMethod.Get))
{
	<div id="headfade">
		@Html.Action("LoggedInAs", "User")
		<br class="clear" />
		<div id="search">
			<input type="text" name="q" placeholder="@SiteStrings.Search_PlaceHolder" />
			<button id="searchbutton">
				<img src="@Url.ThemeContent("search.png", SiteSettings)" alt="Search" />
			</button>
		</div>	
	</div>
}

	<div id="logo">
		<a href="@(Url.Action("Index", "Home"))"><img src="@Url.ThemeContent("logo.png", SiteSettings)" alt="logo" border="0" /></a>
	</div>
	<div id="panel">
		@Html.Action("LeftMenu", "User")
	</div>

	<div id="container">
		@RenderSection("PageToolbar", false)
		@RenderSection("ViewHistoryLink", false)
		<div id="content">
			@RenderBody()
		</div>
	</div>

	<div id="footer">
		@RenderSection("PageDetailsFooter", false)
		<p>
			@SiteStrings.Shared_PoweredBy (@ApplicationSettings.AssemblyVersion.ToString()). This theme contains portions of its design from <a href="http://www.mediawiki.org" target="_blank">Mediawiki.org</a>.
		</p>
	</div>
</body>
</html>